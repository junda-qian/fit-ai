fastapi
mangum
python-dotenv
boto3
requests
pydantic
openai>=1.0.0
# RAG dependencies (faiss-cpu, numpy) excluded - exceeds Lambda 262MB limit
# Options: 1) Use Lambda Layer, 2) Enable OpenSearch, 3) Use without RAG
